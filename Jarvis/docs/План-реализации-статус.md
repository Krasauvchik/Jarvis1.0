# План реализации — статус по фазам

| Фаза | Что | Срок | Статус | Комментарий |
|------|-----|------|--------|-------------|
| **1** | Фундамент (модели, UI, CloudKit) | Неделя 1–2 | ✅ Готово | Модели в `PlannerModels`, UI в `StructuredMainView` + темы. Синхронизация через **iCloud (NSUbiquitousKeyValueStore)**; при необходимости позже можно добавить CloudKit для больших объёмов. |
| **2** | Задачи полностью | Неделя 3–4 | ✅ Готово | CRUD, Inbox, Сегодня, Запланир., Выполнено, категории, теги, повтор, напоминания, поиск, экспорт/импорт. |
| **3** | Google Calendar + Gmail | Неделя 5–6 | ✅ Готово | `CalendarView`, `MailView`, бэкенд OAuth; синхронизация задач с календарём (`CalendarSyncService`). |
| **4** | AI (Gemini + офлайн) | Неделя 7–8 | ✅ Готово | `AIManager`: модель Gemini (через бэкенд), эвристика офлайн; выбор модели в настройках. *Улучшение:* кэш ответов LLM при офлайне. |
| **5** | Здоровье | Неделя 9 | ✅ Готово | `WellnessView`: питание, сон, активность; синхронизация с iCloud. |
| **6** | Аналитика + полировка | Неделя 10 | ✅ Готово | `AnalyticsView`, `TaskStatistics`; анимации, настройки, синхронизация при появлении сети, правила Cursor. |

---

## Дальнейшие шаги (после 10 недель)

1. **Полировка**
   - Проверить доступность (VoiceOver, размер шрифтов).
   - При необходимости — локализация строк (сейчас в основном RU).
   - ProMotion уже учтён (`CADisableMinimumFrameDurationOnPhone` в Info.plist).

2. **AI офлайн**
   - При отсутствии сети в «Глубокий анализ» показывать советы на основе эвристики или кэша последнего ответа LLM.

3. **Опционально: CloudKit**
   - Если понадобится масштабирование или запросы по задачам — перенос ключевых данных с KVS на CloudKit.

4. **Тесты**
   - В проекте есть JarvisTests / JarvisUITests — по мере роста логики добавлять тесты на Store и синхронизацию.

Можно идти дальше по любому из этих пунктов по приоритету.
